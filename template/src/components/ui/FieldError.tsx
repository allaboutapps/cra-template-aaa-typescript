import { styled } from "@mui/system";
import * as React from "react";

export const FieldError = styled("span")({
    color: "#d32f2f",
    display: "block",
    minHeight: 18,
    width: "100%",
    marginTop: 4,
    marginBottom: 10,
    fontSize: 12,
});
